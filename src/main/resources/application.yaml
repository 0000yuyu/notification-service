server:
  port: ${SERVICE_PORT}

spring:
  application:
    name: notification-service
  profiles:
    include:
      - db
      - eureka
      - firebase
      - mail
logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE